sudo: required
language: python
matrix:
  include:
    - python: 3.6
    - python: 3.7
    - python: 3.8

install:
  - pip install -r requirements.txt
  - pip install .

 git:
  depth: 150
   
script:
  - coverage run -m unittest discover

after_success:
  - python -m codecov
  - bash <(curl -s https://codecov.io/bash)

